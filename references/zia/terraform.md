---
product: zia
topic: "zia-terraform"
title: "ZIA Terraform provider resource catalog"
content-type: reference
last-verified: "2026-04-26"
confidence: medium
source-tier: doc
sources:
  - "vendor/terraform-provider-zia/docs/index.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_activation_status.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_admin_roles.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_admin_users.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_advanced_settings.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_advanced_threat_settings.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_atp_malicious_urls.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_atp_malware_inspection.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_atp_malware_policy.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_atp_malware_protocols.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_atp_malware_settings.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_atp_security_exceptions.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_auth_settings_urls.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_bandwidth_classes.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_bandwidth_classes_file_size.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_bandwidth_classes_web_conferencing.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_bandwidth_control_rule.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_browser_control_policy.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_casb_dlp_rules.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_casb_malware_rules.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_cloud_app_control_rule.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_cloud_application_instance.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_cloud_nss_feed.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_custom_file_types.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_dc_exclusions.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_dlp_dictionaries.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_dlp_engines.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_dlp_notification_templates.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_dlp_web_rules.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_email_profile.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_end_user_notification.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_extranet.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_file_type_control_rules.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_firewall_dns_rule.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_firewall_filtering_destination_groups.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_firewall_filtering_ip_source_groups.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_firewall_filtering_network_application_groups.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_firewall_filtering_network_service.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_firewall_filtering_network_service_groups.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_firewall_filtering_rule.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_firewall_ips_rule.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_forwarding_control_proxies.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_forwarding_control_rule.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_forwarding_control_zpa_gateway.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_ftp_control_policy.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_location_management.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_mobile_malware_protection_policy.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_nat_control_rules.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_nss_server.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_risk_profiles.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_rule_labels.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_sandbox_behavioral_analysis.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_sandbox_behavioral_analysis_v2.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_sandbox_file_submission.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_sandbox_rules.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_security_settings.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_ssl_inspection_rules.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_sub_cloud.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_subscription_alert.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_tenant_restriction_profile.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_traffic_capture_rules.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_traffic_forwarding_gre_tunnel.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_traffic_forwarding_static_ip.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_traffic_forwarding_vpn_credentials.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_url_categories.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_url_categories_predefined.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_url_filtering_and_cloud_app_settings.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_url_filtering_rules.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_user_management.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_virtual_service_edge_cluster.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_virtual_service_edge_node.md"
  - "vendor/terraform-provider-zia/docs/resources/zia_workload_groups.md"
author-status: draft
---

# ZIA Terraform provider resource catalog

Complete catalog of the 71 resources and corresponding data sources in the `zscaler/zia` Terraform provider (registry source `zscaler/zia`, v4.x). Resources are grouped by ZIA functional area. Every resource requires activation to take effect; see the [Activation lifecycle](#activation-lifecycle) section.

## Provider overview

The provider wraps the ZIA REST API via `zscaler-sdk-go/v3` and implements the `terraform-plugin-sdk/v2` pattern. The registry source is `zscaler/zia`.

### Authentication — two frameworks

**OneAPI (recommended):** OAuth 2.0 via ZIdentity. Supported since v4.0.0.

```hcl
provider "zia" {
  client_id     = var.client_id
  client_secret = var.client_secret   # or private_key
  vanity_domain = var.vanity_domain
  zscaler_cloud = "beta"              # optional; omit for production
}
```

Environment variables: `ZSCALER_CLIENT_ID`, `ZSCALER_CLIENT_SECRET`, `ZSCALER_PRIVATE_KEY`, `ZSCALER_VANITY_DOMAIN`, `ZSCALER_CLOUD`.

OneAPI is not available on `zscalergov` and `zscalerten` clouds.

**Legacy (backwards-compatible):** Username + password + API key.

```hcl
provider "zia" {
  username          = var.username
  password          = var.password
  api_key           = var.api_key
  zia_cloud         = "zscaler"
  use_legacy_client = true
}
```

Environment variables: `ZIA_USERNAME`, `ZIA_PASSWORD`, `ZIA_API_KEY`, `ZIA_CLOUD`, `ZSCALER_USE_LEGACY_CLIENT`.

Supported legacy clouds: `zscaler`, `zscloud`, `zscalerone`, `zscalertwo`, `zscalerthree`, `zscalerbeta`, `zscalergov`, `zscalerten`, `zspreview`.

### Sandbox authentication (separate)

Sandbox uses its own token independent of the main auth method. Set `ZSCALER_SANDBOX_TOKEN` and `ZSCALER_SANDBOX_CLOUD`.

### Rate limiting

The SDK enforces client-side limits (10 POST/PUT/DELETE and 20 GET per 10-second window) and respects server-side `429 Retry-After` responses. For bulk operations on strictly rate-limited endpoints (e.g., `zia_traffic_forwarding_static_ip`), reduce Terraform parallelism: `terraform apply -parallelism=1`.

### Activation lifecycle

ZIA configuration changes are staged and must be activated before they take effect. Three methods are available:

1. **Implicit activation:** Set environment variable `ZIA_ACTIVATION=true`. The provider activates after each apply automatically.
2. **`zia_activation_status` resource:** Declare an explicit `zia_activation_status` resource with `depends_on` pointing to all managed resources.
3. **Out-of-band CLI:** Run the `ziaActivator` tool separately.

---

## Rule ordering conventions

All rule-based resources (firewall, URL filtering, DLP, SSL inspection, sandbox, CASB, forwarding, NAT, traffic capture, bandwidth control, file type control, IPS) share these constraints:

- `order` must be a positive integer starting at 1 (`ValidateFunc: validation.IntAtLeast(1)`). Negative or zero values create undeletable internal rules.
- Orders must be contiguous with no gaps. When a custom rule is deleted, remaining rules must be renumbered.
- Predefined/default rules can be managed for reordering but cannot be destroyed. Use `terraform apply -target=<resource>` when removing a custom rule and then adjust remaining orders.

---

## Policy: URL Filtering and Cloud App Control

### `zia_url_filtering_rules`

URL filtering policy rules that allow, block, caution, or isolate user web traffic by category, protocol, application, or device group.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `protocols` | List(String) | Required; e.g., `HTTPS_RULE`, `HTTP_RULE` |
| `action` | String | Required; `ALLOW`, `BLOCK`, `CAUTION`, `ICAP_RESPONSE`, `ISOLATE` |
| `url_categories` | List(String) | Optional; predefined or custom category IDs |

Gotcha: `ISOLATE` action requires a Cloud Browser Isolation (CBI) subscription and cannot be combined with other actions in the same rule.

Import: by numeric ID or rule name.

### `zia_url_filtering_and_cloud_app_settings`

Singleton resource for URL and Cloud App Control advanced policy settings. Manages global toggles including Office 365 integration, UCaaS breakout, and generative AI prompt logging.

| Field | Type | Notes |
|---|---|---|
| `enable_dynamic_content_cat` | Bool | Optional |
| `enable_office365` | Bool | Optional |
| `enable_msft_o365` | Bool | Optional |
| `enforce_safe_search` | Bool | Optional |
| `enable_ucaas_zoom` / `_webex` / `_ringcentral` / `_logmein` / `_talkdesk` | Bool | Optional |
| `enable_chatgpt_prompt` / `_gemini_prompt` / `_microsoft_copilot_prompt` / `_poe_prompt` / `_meta_prompt` / `_perplexity_prompt` / `_deep_seek_prompt` / `_claude_prompt` / `_grok_prompt` / `_mistral_ai_prompt` / `_grammarly_prompt` / `_writer_prompt` | Bool | Optional; enable per-AI-app prompt logging |

Gotcha: Only one instance should exist per tenant. Import by the literal string `"app_setting"`.

### `zia_cloud_app_control_rule`

Cloud App Control policy rules that allow, block, or isolate access to specific cloud applications, grouped by application type.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `type` | String | Required; the rule category type (e.g., `SOCIAL_NETWORKING`, `STREAMING`, `AI_PRODUCTIVITY`) |
| `order` | Int | Required, ≥1 |
| `actions` | List(String) | Allow, Block, or Isolate per activity type |
| `applications` | List(String) | Optional; specific cloud app IDs |
| `cbi_profile` | Block | Optional; Cloud Browser Isolation profile |

Gotcha: ISOLATE actions require a CBI subscription and cannot be mixed with other action types in the same rule. Import by compound key `rule_type:rule_id`.

### `zia_cloud_application_instance`

Defines cloud application tenant instances (e.g., corporate-only Slack workspace) that can be referenced in Cloud App Control rules for tenant restriction.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `instance_type` | String | Required |
| `instance_identifiers` | List(String) | Required |

Import: by numeric ID or name.

### `zia_url_categories`

Custom URL categories that can be referenced in URL filtering rules, DLP rules, and SSL inspection rules.

| Field | Type | Notes |
|---|---|---|
| `configured_name` | String | Required |
| `super_category` | String | Required |
| `urls` | List(String) | Optional |
| `keywords` | List(String) | Optional |
| `val` | Int | Computed; numeric ID used when referencing this category in DLP web rules |

Gotcha: A maximum of 48 custom URL categories is allowed per tenant. The `val` attribute (not the string ID) must be used when cross-referencing this category in `zia_dlp_web_rules`. Import by numeric ID or name.

### `zia_url_categories_predefined`

Augments existing predefined URL categories by adding or removing URLs and keywords without creating a new category. Does not support create or delete; changes are incremental using `ADD_TO_LIST` or `REMOVE_FROM_LIST`.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required; must match an existing predefined category ID |
| `urls_retaining_parent_category_to_add` | List(String) | Optional |
| `urls_retaining_parent_category_to_remove` | List(String) | Optional |

Gotcha: The `description` field is not supported by the API for predefined categories; setting it will cause drift. This is an Early Access feature and may not be available in all tenants.

### `zia_risk_profiles`

Cloud application risk profiles that classify cloud apps by security attributes (data encryption, MFA, admin audit, etc.) for use in Cloud App Control rules.

| Field | Type | Notes |
|---|---|---|
| `profile_name` | String | Required |
| `certifications` | List(String) | Optional |
| `poor_items_of_service` | Bool | Optional |

Import: by numeric ID or name.

### `zia_tenant_restriction_profile`

Tenant restriction profiles that limit which organizational tenants users can access within a SaaS application.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `app_type` | String | Optional |
| `item_type_primary` | String | Optional |
| `item_data_primary` | List(String) | Optional |

Import: by numeric ID or name.

---

## Policy: Firewall

### `zia_firewall_filtering_rule`

Cloud Firewall policy rules that allow, block, or drop network traffic based on source/destination IP, port, protocol, application, user, and time.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `action` | String | Required; `ALLOW`, `BLOCK_DROP`, `BLOCK_RESET`, `BLOCK_ICMP`, `EVAL_NWAPP` |
| `src_ips` | List(String) | Optional |
| `dest_addresses` | List(String) | Optional |

Gotcha: Predefined rules cannot be destroyed. Destruction of predefined rules is silently ignored by the provider. Import by numeric ID or name.

### `zia_firewall_dns_rule`

DNS Control policy rules that allow, block, or redirect DNS queries based on domain, category, or application.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `action` | String | Required; `ALLOW`, `BLOCK`, `REDIR_REQ`, `REDIR_RES`, `REDIR_BOTH` |
| `redirect_ip` | String | Optional; required for redirect actions |
| `dest_ip_categories` | List(String) | Optional |

Gotcha: When either `dest_ip_categories` or `res_categories` is set, both fields must be set and must match. Import by numeric ID or name.

### `zia_firewall_ips_rule`

IPS Control policy rules that allow, block, reset, or bypass intrusion prevention for traffic matching threat categories.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `action` | String | Required; `ALLOW`, `BLOCK_DROP`, `BLOCK_RESET`, `BYPASS_IPS` |
| `threat_categories` | List(String) | Optional |

Import: by numeric ID or name.

### `zia_nat_control_rules`

DNAT (Destination NAT) rules that redirect traffic to a different IP, port, or FQDN.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `redirect_ip` | String | Optional |
| `redirect_port` | Int | Optional |
| `redirect_fqdn` | String | Optional |

Import: by numeric ID or name.

### `zia_firewall_filtering_destination_groups`

IP destination groups that classify destination IP addresses or custom URL categories for use in firewall rules.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `type` | String | Required; `DSTN_IP`, `DSTN_FQDN`, `DSTN_DOMAIN`, `DSTN_OTHER` |
| `addresses` | List(String) | Optional |
| `ip_categories` | List(String) | Optional |

Gotcha: The `ip_categories` field accepts only custom URL categories, not predefined ones. Import by numeric ID or name.

### `zia_firewall_filtering_ip_source_groups`

IP source groups that classify source IP address ranges for use in firewall rules.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `ip_addresses` | List(String) | Required |

Import: by numeric ID or name.

### `zia_firewall_filtering_network_service`

Network service objects defining protocol and port combinations for use in firewall rules.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `type` | String | Required; `STANDARD`, `PREDEFINED`, `CUSTOM` |
| `src_tcp_ports` / `dest_tcp_ports` | List(Block) | Optional; port range blocks with `start` and `end` |

Gotcha: PREDEFINED services must be imported first using Zscaler-Terraformer; they cannot be created by the provider. Import by numeric ID or name.

### `zia_firewall_filtering_network_service_groups`

Groups of network service objects for bulk reference in firewall rules.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `services` | Block | Required; list of service ID references |

Import: by numeric ID or name.

### `zia_firewall_filtering_network_application_groups`

Groups of cloud network application identifiers for firewall rule matching.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `network_applications` | List(String) | Required |

Import: by numeric ID or name.

---

## Policy: DLP

### `zia_dlp_web_rules`

DLP web policy rules that inspect outbound web traffic for sensitive content and apply block, allow, quarantine, or watermark actions.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `action` | String | Required; `ALLOW`, `BLOCK`, `ICAP_RESPONSE`, `QUARANTINE`, etc. |
| `file_types` | List(String) | Optional |
| `dlp_engines` | List(Block) | Optional; engine IDs |
| `url_categories` | List(Block) | Optional; reference category by `val` (numeric) |

Gotcha: Maximum of 127 DLP web rules. URL categories must be referenced by their numeric `val` attribute, not by name string. Parent/sub-rule hierarchy is supported. Import by numeric ID or name.

### `zia_dlp_dictionaries`

DLP dictionaries containing phrases, patterns, EDM, or IDM entries used in DLP engines and rules.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `dictionary_type` | String | Required; `TEXT`, `REGEX`, `EXACT_DATA_MATCH`, `INDEXED_DATA_MATCH` |
| `phrases` | List(Block) | Optional; action + phrase pairs |
| `patterns` | List(Block) | Optional; action + pattern pairs |

Import: by numeric ID or name.

### `zia_dlp_engines`

Custom DLP engines that combine dictionaries using logical expressions to drive DLP rule matching.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `engine_expression` | String | Optional; logical expression over dictionary IDs |
| `custom_dlp_engine` | Bool | Optional |

Gotcha: POST/PUT/DELETE on custom DLP engines requires explicit enablement by Zscaler Support. Import by numeric ID or name.

### `zia_dlp_notification_templates`

DLP notification templates used to send email alerts to users or administrators when a DLP policy violation is detected.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `plain_text_message` | String | Required |
| `html_message` | String | Required |
| `subject` | String | Optional |

Import: by numeric ID or name.

### `zia_casb_dlp_rules`

SaaS Security API (CASB) data-at-rest scanning DLP rules that inspect files stored in connected cloud applications.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `type` | String | Required; `OFLCASB_DLP_*` enum |
| `action` | String | Required; `OFLCASB_DLP_*` enum |
| `cloud_app_tenants` | List(Block) | Required |

Import: by compound key `rule_type:rule_id`.

### `zia_casb_malware_rules`

SaaS Security API (CASB) data-at-rest scanning malware detection rules that inspect files stored in connected cloud applications.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `type` | String | Required; `OFLCASB_AVP_*` enum |
| `action` | String | Required; `OFLCASB_AVP_*` enum |
| `cloud_app_tenants` | List(Block) | Required |

Import: by compound key `rule_type:rule_id`.

### `zia_email_profile`

Email recipient profiles used in DLP notification templates to define groups of email recipients for violation alerts.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `emails` | List(String) | Optional |

Gotcha: This is an Early Access feature and may not be available in all tenants. Import by numeric ID or name.

---

## Policy: SSL Inspection

### `zia_ssl_inspection_rules`

SSL inspection policy rules that decrypt, bypass, or block HTTPS traffic based on category, application, user, or location.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `action` | Block | Required; nested block with `type`: `DECRYPT`, `DO_NOT_DECRYPT`, `BLOCK` |
| `ssl_interception_cert` | Block | Optional; certificate ID for interception |
| `url_categories` | List(Block) | Optional |

Import: by numeric ID or name.

---

## Policy: Sandbox

### `zia_sandbox_rules`

Sandbox policy rules that control which file types are submitted to the cloud sandbox for behavioral analysis.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `ba_rule_action` | String | Optional; `QUARANTINE`, `ALLOW`, `SCAN_AND_DELIVER` |
| `first_time_operation` | String | Optional; behavior on first-seen files |
| `ba_policy_categories` | List(String) | Optional |
| `file_types` | List(String) | Optional |

Import: by numeric ID or name.

### `zia_sandbox_behavioral_analysis`

Legacy singleton resource for the sandbox MD5 hash blocklist. Manages the list of MD5 hashes that sandbox always blocks.

| Field | Type | Notes |
|---|---|---|
| `file_hashes_to_be_blocked` | List(String) | Required; list of MD5 hashes |

Import: by the literal string `"sandbox_settings"`. Superseded by `zia_sandbox_behavioral_analysis_v2` for new configurations.

### `zia_sandbox_behavioral_analysis_v2`

New-format singleton resource for the sandbox MD5 hash list, supporting type-tagged entries.

| Field | Type | Notes |
|---|---|---|
| `md5_hash_value_list` | List(Block) | Optional; blocks with `md5_hash_value` and `type` |

Gotcha: This is an Early Access feature. It supersedes `zia_sandbox_behavioral_analysis` but the two coexist in the provider. Import by the literal string `"sandbox_settings"`.

### `zia_sandbox_file_submission`

Submits a file to the Zscaler cloud sandbox for analysis. This resource is not a typical CRUD resource — it performs a file submission on create and has no read, update, or import.

| Field | Type | Notes |
|---|---|---|
| `file_path` | String | Required; local path to the file |
| `submission_method` | String | Required; `submit` or `discan` |
| `force` | Bool | Optional; re-submit even if previously analyzed |

Gotcha: Requires `ZSCALER_SANDBOX_TOKEN` and a Sandbox license. Not importable.

---

## Policy: Bandwidth Control

### `zia_bandwidth_control_rule`

Bandwidth control policy rules that enforce upload and download speed limits on user traffic based on application, category, or protocol.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `min_bandwidth` | Int | Optional; Kbps |
| `max_bandwidth` | Int | Optional; Kbps |
| `protocols` | List(String) | Optional |
| `bandwidth_classes` | List(Block) | Optional |

Gotcha: Requires Zscaler OneAPI authentication. Not supported with the legacy API framework. Import by numeric ID or name.

### `zia_bandwidth_classes`

Bandwidth class objects that group URLs, URL categories, or web applications for reference in bandwidth control rules.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `urls` | List(String) | Optional |
| `url_categories` | List(String) | Optional |
| `web_applications` | List(String) | Optional |

Gotcha: Requires Zscaler OneAPI. Import by numeric ID or name.

### `zia_bandwidth_classes_file_size`

Bandwidth class objects that classify traffic by file size for use in bandwidth control rules.

| Field | Type | Notes |
|---|---|---|
| `file_size` | String | Required; enum: `FILE_5MB`, `FILE_10MB`, `FILE_50MB`, `FILE_100MB`, `FILE_500MB`, `FILE_1GB` |

Gotcha: Requires Zscaler OneAPI. Import by numeric ID or name.

### `zia_bandwidth_classes_web_conferencing`

Bandwidth class objects that classify web conferencing or VoIP application traffic for bandwidth control.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `type` | String | Required; `BANDWIDTH_CAT_WEBCONF` or `BANDWIDTH_CAT_VOIP` |
| `applications` | List(String) | Optional |

Gotcha: Requires Zscaler OneAPI. Import by numeric ID or name.

---

## Policy: File Type Control

### `zia_file_type_control_rules`

File Type Control policy rules that allow or block file transfers based on file type and direction.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `filtering_action` | String | Required; `ALLOW` or `BLOCK` |
| `operation` | String | Optional; `DOWNLOAD`, `UPLOAD`, or both |
| `file_types` | List(String) | Optional |

Import: by numeric ID or name.

### `zia_custom_file_types`

Custom file type definitions that extend the built-in file type library for use in File Type Control rules.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `extension` | String | Required; file extension (e.g., `xyz`) |
| `category` | String | Optional |

Import: by numeric ID or name.

---

## Policy: Traffic Capture

### `zia_traffic_capture_rules`

Traffic capture policy rules that define which transactions are captured for security analysis, with size and sampling controls.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `txn_size_limit` | Int | Optional; maximum captured payload bytes |
| `txn_sampling` | Int | Optional; percentage of matching transactions to capture |

Import: by numeric ID or name.

---

## Forwarding: Forwarding Control

### `zia_forwarding_control_rule`

Forwarding control rules that direct traffic to a specific forwarding method: ZIA cloud, a proxy, or a ZPA app segment.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `order` | Int | Required, ≥1 |
| `type` | String | Required; `FORWARDING` |
| `forward_method` | String | Required; `DIRECT`, `PROXYCHAIN`, `ZPA`, `IPSEC_TUNNEL`, `GRE_TUNNEL`, `ECSELF`, `ZIA` |
| `zpa_gateway` | Block | Optional; required for `ZPA` method |

Gotcha: When `forward_method = "ZPA"`, the provider pauses 60 seconds and retries up to 3 times to allow ZPA gateway propagation. Import by numeric ID or name.

### `zia_forwarding_control_zpa_gateway`

ZPA gateway objects that define which ZPA server group and application segments are used when forwarding to ZPA via Forwarding Control.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `zpa_server_group` | Block | Required; ZPA server group ID |
| `zpa_app_segments` | List(Block) | Required; ZPA application segment IDs |

Gotcha: Requires a configured ZPA Terraform provider (`zpa` provider) to manage the referenced server groups and app segments. Import by numeric ID or name.

### `zia_forwarding_control_proxies`

Third-party proxy configuration objects used in Forwarding Control rules to chain traffic through an external proxy.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `address` | String | Required; proxy IP or hostname |
| `port` | Int | Required |
| `type` | String | Required; `PROXYCHAIN`, `ZIA`, `ECSELF` |

Import: by numeric ID or name.

---

## Forwarding: Traffic Forwarding (Tunnels and VPN)

### `zia_traffic_forwarding_gre_tunnel`

GRE tunnel objects that forward location traffic to Zscaler enforcement nodes.

| Field | Type | Notes |
|---|---|---|
| `source_ip` | String | Required; public IP at the customer end |
| `ip_unnumbered` | Bool | Optional |
| `primary_dest_vip` | Block | Optional; primary Zscaler VIP ID |
| `secondary_dest_vip` | Block | Optional; secondary Zscaler VIP ID |

Gotcha: The provider automatically queries for recommended VIP pairs if none are specified. Import by numeric ID or the source IP address.

### `zia_traffic_forwarding_static_ip`

Static IP address objects that identify a customer IP for association with locations and VPN credentials.

| Field | Type | Notes |
|---|---|---|
| `ip_address` | String | Required |
| `geo_override` | Bool | Optional |
| `latitude` / `longitude` | Float | Optional |

Gotcha: As of v4.6.2, geographic coordinates are auto-determined from the IP address when `geo_override = false`. Setting `geo_override = true` with explicit coordinates is the recommended pattern for precise control. The `/staticIP` endpoint is rate-limited to 1 POST per second; use `terraform apply -parallelism=1` for bulk creation. Import by numeric ID or IP address.

### `zia_traffic_forwarding_vpn_credentials`

VPN credential objects (UFQDN or IP type) used to authenticate IPSec tunnels from locations.

| Field | Type | Notes |
|---|---|---|
| `type` | String | Required; `UFQDN` or `IP` |
| `fqdn` | String | Conditional; required when `type = UFQDN` |
| `ip_address` | String | Conditional; required when `type = IP` |
| `pre_shared_key` | String | Required; sensitive |

Gotcha: IP type credentials require a corresponding `zia_traffic_forwarding_static_ip` object to exist first. The `pre_shared_key` is write-only and not returned by the API; Terraform preserves the prior state value on read. Import by numeric ID or FQDN/IP.

---

## Forwarding: Locations

### `zia_location_management`

Location and sub-location objects that represent physical or logical network locations forwarding traffic to Zscaler.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `ip_addresses` | List(String) | Conditional; required unless using VPN credentials |
| `vpn_credentials` | List(Block) | Conditional; required unless using IP addresses |
| `parent_id` | Int | Optional; parent location ID for sub-locations |
| `auth_required` | Bool | Optional |

Gotcha: A location must have either `ip_addresses` or `vpn_credentials` (or both). Sub-locations are created by specifying `parent_id`. Import by numeric ID or name.

---

## Policy: Advanced Threat Protection

### `zia_advanced_threat_settings`

Singleton resource for Advanced Threat Protection (ATP) policy settings controlling block behavior for malicious content categories.

| Field | Type | Notes |
|---|---|---|
| `risk_tolerance` | Int | Optional; 1–5 scale |
| `cmd_ctl_server_blocked` | Bool | Optional |
| `blocked_countries` | List(String) | Optional; ISO country codes |
| `block_unknown_regions` | Bool | Optional |

Import: by the literal string `"advanced_threat_settings"`.

### `zia_atp_malicious_urls`

Singleton resource for the ATP malicious URL denylist that blocks access to known-malicious URLs.

| Field | Type | Notes |
|---|---|---|
| `malicious_urls` | Set(String) | Required |

Import: by the literal string `"all_urls"`.

### `zia_atp_security_exceptions`

Singleton resource for the ATP security exceptions (allowlist) that bypasses ATP scanning for specific URLs.

| Field | Type | Notes |
|---|---|---|
| `bypass_urls` | List(String) | Required |

Import: by the literal string `"security_exceptions"`.

### `zia_atp_malware_settings`

Singleton resource for ATP malware category block and capture toggles (virus, trojans, ransomware, spyware, etc.).

| Field | Type | Notes |
|---|---|---|
| `block_ransomware` | Bool | Optional |
| `block_virus` | Bool | Optional |
| `capture_ransomware` | Bool | Optional |

Import: by the literal string `"malware_settings"`.

### `zia_atp_malware_policy`

Singleton resource for ATP malware policy governing block behavior for unscannable and password-protected archive files.

| Field | Type | Notes |
|---|---|---|
| `block_unscannable` | Bool | Optional |
| `block_password_protected_archives` | Bool | Optional |

Import: by the literal string `"malware_policy"`.

### `zia_atp_malware_inspection`

Singleton resource for ATP malware scan direction toggles controlling whether inbound and/or outbound traffic is inspected.

| Field | Type | Notes |
|---|---|---|
| `inspect_inbound` | Bool | Optional |
| `inspect_outbound` | Bool | Optional |

Import: by the literal string `"malware_inspection"`.

### `zia_atp_malware_protocols`

Singleton resource for ATP protocol-level malware scan toggles (HTTP, FTP, FTP-over-HTTP).

| Field | Type | Notes |
|---|---|---|
| `http_scan_enabled` | Bool | Optional |
| `ftp_scan_enabled` | Bool | Optional |
| `ftp_over_http_scan_enabled` | Bool | Optional |

Import: by the literal string `"malware_protocols"`.

### `zia_mobile_malware_protection_policy`

Singleton resource for the mobile malware protection policy that controls malware block behavior for mobile app traffic.

| Field | Type | Notes |
|---|---|---|
| Multiple boolean block flags | Bool | Optional; one per mobile malware category |

Import: by the literal string `"mobile_malware_protection_policy"`.

---

## Identity: Admin and Roles

### `zia_admin_users`

Local ZIA administrator accounts with assigned roles and scope restrictions.

| Field | Type | Notes |
|---|---|---|
| `login_name` | String | Required; email format |
| `username` | String | Required |
| `password` | String | Required; sensitive |
| `role` | Block | Required; role ID reference |
| `admin_scope_type` | String | Optional; `ORGANIZATION`, `LOCATION`, `LOCATION_GROUP`, `DEPARTMENT` |

Gotcha: The `password` attribute is write-only and is not returned by the API on import or read; Terraform preserves the prior state value. Import by numeric ID or `login_name`.

### `zia_admin_roles`

ZIA administrator roles defining which ZIA features the role holder can access and modify.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `rank` | Int | Optional; 1–7 scale, default 7 (least privileged) |
| Various permission fields | List(String) | Optional; e.g., `policy_access`, `report_access`, `dashboard_access` |

Gotcha: Rank 1 is most privileged. Lower-ranked admins cannot manage higher-ranked admins. Import by numeric ID or name.

### `zia_auth_settings_urls`

Singleton resource for the cookie-based authentication exemption list — URLs in this list bypass ZIA user authentication.

| Field | Type | Notes |
|---|---|---|
| `urls` | List(String) | Required |

Import: by the literal string `"auth_settings_urls"`.

---

## Identity: User Management

### `zia_user_management`

Local ZIA user accounts that can be referenced in policies for user-based enforcement.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `email` | String | Required; must be unique within the org domain |
| `password` | String | Required; sensitive |
| `groups` | List(Block) | Required; group ID references |
| `department` | Block | Required; department ID reference |
| `auth_methods` | List(String) | Optional; `BASIC` |

Gotcha: The `password` attribute is write-only and is not imported. Import by numeric ID or name.

---

## Logging: NSS and Cloud NSS

### `zia_nss_server`

NSS (Nanolog Streaming Service) server objects that represent on-premises NSS appliances receiving log streams from Zscaler.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `type` | String | Optional; `NSS_FOR_WEB` or `NSS_FOR_FIREWALL` |
| `status` | String | Optional; `ENABLED` or `DISABLED` |

Import: by numeric ID or name.

### `zia_cloud_nss_feed`

Cloud NSS feed objects that stream ZIA logs directly to a SIEM or other external endpoint without an on-premises NSS appliance.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `nss_log_type` | String | Required; log type (e.g., `WEB`, `FIREWALL`, `DNS`) |
| `nss_feed_type` | String | Required |
| `siem_type` | String | Optional; `SPLUNK`, `SUMO_LOGIC`, `OTHER`, etc. |
| `connection_url` | String | Optional |

Import: by numeric ID or name.

---

## Settings: Global and Advanced

### `zia_advanced_settings`

Singleton resource for ZIA global advanced settings. Contains approximately 50 boolean and set-based configuration fields governing inspection, caching, and platform behaviour.

| Field | Type | Notes |
|---|---|---|
| `enable_office365` | Bool | Optional |
| `log_internal_ip` | Bool | Optional |
| `auth_bypass_urls` | List(String) | Optional |

Import: by the literal string `"advanced_settings"`.

### `zia_security_settings`

Singleton resource for the global URL security allowlist and denylist.

| Field | Type | Notes |
|---|---|---|
| `whitelist_urls` | List(String) | Optional; max 255 entries |
| `blacklist_urls` | List(String) | Optional; max 275,000 entries |

Import: by the literal string `"security_settings"`.

### `zia_browser_control_policy`

Singleton resource for browser control settings including plugin check frequency, browser version block lists, and Smart Browser Isolation profile assignment.

| Field | Type | Notes |
|---|---|---|
| `plugin_check_frequency` | String | Optional |
| `enable_smart_browser_isolation` | Bool | Optional |
| `smart_isolation_profile` | Block | Optional; CBI profile reference |

Import: by the literal string `"browser_control_policy"`.

### `zia_end_user_notification`

Singleton resource for browser-based end user notifications (EUN) displayed to users when traffic is blocked or cautioned.

| Field | Type | Notes |
|---|---|---|
| `notification_type` | String | Optional; `DEFAULT` or `CUSTOM` |
| `aup_message` | String | Optional |
| `caution_again_after` | Int | Optional; minutes before re-showing caution |

Gotcha: JavaScript is not allowed in custom notification text. Import by the literal string `"end_user_notification"`.

### `zia_ftp_control_policy`

Singleton resource for FTP Control policy settings governing FTP and FTP-over-HTTP traffic.

| Field | Type | Notes |
|---|---|---|
| `ftp_enabled` | Bool | Optional |
| `ftp_over_http_enabled` | Bool | Optional |
| `url_categories` | List(String) | Optional |
| `urls` | List(String) | Optional |

Import: by the literal string `"ftp_control_policy"`.

### `zia_subscription_alert`

Alert subscription objects that send email notifications to specified recipients when ZIA generates system alerts.

| Field | Type | Notes |
|---|---|---|
| `email` | String | Required |
| `critical` | List(String) | Optional; alert classes to notify on CRITICAL |
| `warning` | List(String) | Optional; alert classes to notify on WARNING |
| `info` | List(String) | Optional; alert classes to notify on INFO |

Import: by numeric ID or email address.

### `zia_rule_labels`

Rule label objects used to tag and categorize rules for bulk management and reporting.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `description` | String | Optional |

Import: by numeric ID or name.

### `zia_activation_status`

Explicit activation trigger resource. Declaring this resource with `depends_on` pointing to all other managed resources causes Terraform to activate ZIA configuration after apply completes.

| Field | Type | Notes |
|---|---|---|
| `status` | String | Required; must be `"ACTIVE"` |

Gotcha: This resource is not importable. It does not represent durable API state; use it only when implicit activation via `ZIA_ACTIVATION=true` is not desired.

---

## Infrastructure: Virtual Service Edges

### `zia_virtual_service_edge_node`

Virtual Service Edge (VZEN) node objects representing individual Zscaler service edge appliance instances.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Optional |
| `status` | String | Optional; `ENABLED` (default) or `DISABLED` |
| `type` | String | Optional; `VZEN` |
| `ip_address` | String | Optional; IPv4 only |
| `subnet_mask` | String | Optional |
| `default_gateway` | String | Optional |
| `deployment_mode` | String | Optional; `STANDALONE` or `CLUSTER` |
| `vzen_sku_type` | String | Optional; `SMALL`, `MEDIUM`, `LARGE` |
| `load_balancer_ip_address` | String | Optional; required when `deployment_mode = CLUSTER` |

Gotcha: `load_balancer_ip_address` is only applicable in `CLUSTER` mode. Import by numeric ID or node name.

### `zia_virtual_service_edge_cluster`

Virtual Service Edge cluster objects grouping VZEN nodes for high availability.

| Field | Type | Notes |
|---|---|---|
| `virtual_zen_nodes` | List(Block) | Optional; VZEN node ID references |
| `ip_address` | String | Optional |
| `ip_sec_enabled` | Bool | Optional |

Import: by numeric ID or name.

---

## Infrastructure: Workload Groups

### `zia_workload_groups`

Workload group objects that classify cloud workloads by VM, VPC, ENI, subnet, or attribute tags for policy matching in Firewall, URL Filtering, and DLP rules.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `description` | String | Optional |
| `expression_json` | Block | Optional; nested `expression_containers` with `tag_type`, `operator`, and `tags` key-value pairs |

Gotcha: A maximum of 8 tags can be added per workload group across all tag types. Import by numeric ID or name.

---

## Infrastructure: Extranet and Subclouds (Early Access)

### `zia_extranet`

Extranet configuration objects that define DNS and IP pool settings for extending ZIA enforcement to extranet networks.

| Field | Type | Notes |
|---|---|---|
| `name` | String | Required |
| `extranet_dns_list` | List(Block) | Optional |
| `extranet_ip_pool_list` | List(Block) | Optional |

Gotcha: Early Access feature. Import by numeric ID or name.

### `zia_dc_exclusions`

Data center exclusion time windows that temporarily exclude specific Zscaler data centers from serving an organization's traffic.

| Field | Type | Notes |
|---|---|---|
| `datacenter_id` | Int | Required |
| `start_time` | String | Optional |
| `end_time` | String | Optional |

Gotcha: Early Access feature. Import by numeric ID.

### `zia_sub_cloud`

Subcloud data center exclusion management. Controls which subclouds are excluded from forwarding for an organization.

| Field | Type | Notes |
|---|---|---|
| `cloud_id` | Int | Required |
| `exclusions` | List(Block) | Optional |

Gotcha: Early Access feature. Import by numeric ID.

---

## Data Sources

Data sources have the same names as corresponding resources (prefix `data.zia_*`) and look up existing objects by `name` or `id`. The table below lists every data source and what it reads. Data sources with JMESPath `search` support are noted.

### Identity and Users

| Data Source | Reads |
|---|---|
| `zia_admin_roles` | Admin role by name or ID |
| `zia_admin_users` | Admin user by `login_name` or ID |
| `zia_department_management` | User department by name or ID. Supports `search` (JMESPath) |
| `zia_group_management` | User group by name or ID. Supports `search` (JMESPath) |
| `zia_user_management` | Local user by name, email, or ID. Supports `search` (JMESPath) |

### Policy: URL Filtering and Cloud App Control

| Data Source | Reads |
|---|---|
| `zia_url_filtering_rules` | URL filtering rule by name or ID |
| `zia_url_filtering_and_cloud_app_settings` | URL and Cloud App Control advanced settings singleton |
| `zia_url_categories` | URL category (custom or predefined) by name or ID |
| `zia_cloud_app_control_rule` | Cloud App Control rule by name, type, and ID |
| `zia_cloud_app_control_rule_actions` | Available actions for a given Cloud App Control rule type |
| `zia_cloud_application_instance` | Cloud application tenant instance by name or ID |
| `zia_cloud_applications` | Predefined and custom cloud application catalog entries |
| `zia_risk_profiles` | Cloud application risk profile by name or ID |
| `zia_tenant_restriction_profile` | Tenant restriction profile by name or ID |

### Policy: Firewall

| Data Source | Reads |
|---|---|
| `zia_firewall_filtering_rule` | Firewall filtering rule by name or ID |
| `zia_firewall_dns_rule` | Firewall DNS Control rule by name or ID |
| `zia_firewall_ips_rule` | Firewall IPS Control rule by name or ID |
| `zia_nat_control_rules` | NAT Control (DNAT) rule by name or ID |
| `zia_firewall_filtering_destination_groups` | IP destination group by name or ID |
| `zia_firewall_filtering_ip_source_groups` | IP source group by name or ID |
| `zia_firewall_filtering_network_service` | Network service by name or ID |
| `zia_firewall_filtering_network_service_groups` | Network service group by name or ID |
| `zia_firewall_filtering_network_application` | Predefined network application by name or ID |
| `zia_firewall_filtering_network_application_groups` | Network application group by name or ID |
| `zia_firewall_filtering_application_services` | Firewall application service by name or ID |
| `zia_firewall_filtering_application_services_group` | Firewall application service group by name or ID |
| `zia_firewall_filtering_time_window` | Time window by name or ID |

### Policy: DLP

| Data Source | Reads |
|---|---|
| `zia_dlp_web_rules` | DLP web rule by name or ID |
| `zia_dlp_dictionaries` | DLP dictionary by name or ID |
| `zia_dlp_dictionary_predefined_identifiers` | Predefined identifier list for a DLP dictionary |
| `zia_dlp_engines` | DLP engine by name or ID |
| `zia_dlp_notification_templates` | DLP notification template by name or ID |
| `zia_dlp_icap_servers` | DLP ICAP server by name or ID |
| `zia_dlp_idm_profiles` | IDM (Indexed Document Match) profile by name or ID |
| `zia_dlp_idm_profile_lite` | IDM profile lite (summary) by name or ID |
| `zia_dlp_edm_schema` | EDM (Exact Data Match) schema/template by name or ID |
| `zia_dlp_incident_receiver_servers` | DLP incident receiver server by name or ID |
| `zia_dlp_cloud_to_cloud_ir` | Cloud-to-Cloud Incident Receiver configuration |
| `zia_email_profile` | Email recipient profile by name or ID |

### Policy: SaaS Security (CASB)

| Data Source | Reads |
|---|---|
| `zia_casb_dlp_rules` | CASB DLP data-at-rest rule by type and ID |
| `zia_casb_malware_rules` | CASB malware data-at-rest rule by type and ID |
| `zia_casb_email_label` | Email label for SaaS Security API policies |
| `zia_casb_tenant` | SaaS application tenant by name or ID |
| `zia_casb_tombstone_template` | Quarantine tombstone file template |
| `zia_domain_profiles` | Domain profile summary |

### Policy: SSL Inspection

| Data Source | Reads |
|---|---|
| `zia_ssl_inspection_rules` | SSL inspection rule by name or ID |

### Policy: Sandbox

| Data Source | Reads |
|---|---|
| `zia_sandbox_rules` | Sandbox policy rule by name or ID |
| `zia_sandbox_behavioral_analysis` | MD5 hash blocklist (legacy) |
| `zia_sandbox_behavioral_analysis_v2` | MD5 hash list (new format, Early Access) |
| `zia_sandbox_report` | Full or summary analysis report for an MD5 hash |

### Policy: Bandwidth Control

| Data Source | Reads |
|---|---|
| `zia_bandwidth_control_rule` | Bandwidth control rule by name or ID |
| `zia_bandwidth_classes` | Bandwidth class by name or ID |

### Policy: File Type Control

| Data Source | Reads |
|---|---|
| `zia_file_type_control_rules` | File type control rule by name or ID |
| `zia_custom_file_types` | Custom file type by name or ID |
| `zia_file_type_categories` | Built-in file type category list |

### Policy: Traffic Capture

| Data Source | Reads |
|---|---|
| `zia_traffic_capture_rules` | Traffic capture rule by name or ID |

### Forwarding Control

| Data Source | Reads |
|---|---|
| `zia_forwarding_control_rule` | Forwarding control rule by name or ID |
| `zia_forwarding_control_zpa_gateway` | ZPA gateway object by name or ID |
| `zia_forwarding_control_proxies` | Third-party proxy object by name or ID |
| `zia_forwarding_control_proxy_gateway` | Proxy gateway object by name or ID |
| `zia_dedicated_ip_proxy` | Dedicated IP gateway from forwarding control |

### Traffic Forwarding

| Data Source | Reads |
|---|---|
| `zia_traffic_forwarding_gre_tunnel` | GRE tunnel by source IP or ID |
| `zia_traffic_forwarding_gre_tunnel_info` | IP addresses with GRE tunnel details |
| `zia_traffic_forwarding_gre_vip_recommended_list` | Recommended GRE VIPs for a source IP or coordinates |
| `zia_traffic_forwarding_public_node_vips` | Paginated list of all Zscaler public VIPs |
| `zia_traffic_forwarding_static_ip` | Static IP by address or ID |
| `zia_traffic_forwarding_vpn_credentials` | VPN credentials by FQDN, IP, or ID |
| `zia_gre_internal_ip_range_list` | Available GRE tunnel internal IP ranges |

### Location Management

| Data Source | Reads |
|---|---|
| `zia_location_management` | Location by name or ID. Supports `search` (JMESPath) |
| `zia_location_lite` | Location summary (lite) by name or ID |
| `zia_location_groups` | Location group by name or ID. Supports `search` (JMESPath) |
| `zia_sub_location_management` | Sub-location by name or ID |

### Settings

| Data Source | Reads |
|---|---|
| `zia_advanced_settings` | Global advanced settings singleton |
| `zia_advanced_threat_settings` | ATP advanced settings singleton |
| `zia_security_settings` | URL allowlist and denylist |
| `zia_browser_control_policy` | Browser control settings singleton |
| `zia_end_user_notification` | End user notification settings singleton |
| `zia_ftp_control_policy` | FTP control policy singleton |
| `zia_mobile_malware_protection_policy` | Mobile malware protection policy singleton |
| `zia_auth_settings_urls` | Cookie auth exemption URL list |
| `zia_subscription_alert` | Alert subscription by email or ID |

### NSS and Logging

| Data Source | Reads |
|---|---|
| `zia_nss_server` | NSS server by name or ID |
| `zia_cloud_nss_feed` | Cloud NSS feed by name or ID |

### Cloud Browser Isolation

| Data Source | Reads |
|---|---|
| `zia_cloud_browser_isolation_profile` | CBI profile by name or ID |

### Devices

| Data Source | Reads |
|---|---|
| `zia_devices` | Device by name, OS, or other filter. Supports `search` (JMESPath) |
| `zia_device_groups` | Device group by name or ID |

### Workload Groups

| Data Source | Reads |
|---|---|
| `zia_workload_groups` | Workload group by name or ID |

### Rule Labels

| Data Source | Reads |
|---|---|
| `zia_rule_labels` | Rule label by name or ID |

### Virtual Service Edges

| Data Source | Reads |
|---|---|
| `zia_virtual_service_edge_cluster` | VSE cluster by name or ID |
| `zia_virtual_service_edge_node` | VSE node by name or ID |

### Infrastructure (Early Access / Reference)

| Data Source | Reads |
|---|---|
| `zia_extranet` | Extranet configuration by name or ID |
| `zia_dc_exclusions` | Data center exclusion windows |
| `zia_sub_cloud` | Subcloud exclusion configuration by ID |
| `zia_datacenters` | List of Zscaler data centers available for exclusion |

### Activation

| Data Source | Reads |
|---|---|
| `zia_activation_status` | Current activation status of staged configuration changes |

---

## Open questions / clarifications register

1. **Resolved 2026-04-26.** Complete enum lists confirmed from provider source:
   - `zia_bandwidth_classes_file_size` — `file_size` values: `FILE_5MB`, `FILE_10MB`, `FILE_50MB`, `FILE_100MB`, `FILE_250MB`, `FILE_500MB`, `FILE_1GB`. Source: `vendor/terraform-provider-zia/zia/resource_zia_bandwidth_classes_file_size.go` (lines 74–80).
   - `zia_bandwidth_classes_web_conferencing` — `type` values: `BANDWIDTH_CAT_WEBCONF`, `BANDWIDTH_CAT_VOIP`. Source: `vendor/terraform-provider-zia/zia/resource_zia_bandwidth_classes_web_conferencing.go` (lines 94–95).

2. **`zia_url_categories_predefined`** — Early Access status and tenant eligibility criteria are not documented in the provider source. Availability may vary by ZIA edition and cloud environment. Remains unresolved.

3. **`zia_sandbox_behavioral_analysis_v2`** — the exact relationship between v1 and v2 (whether they write to the same underlying API endpoint or separate ones) is not confirmed in the provider docs. Both resources import using the same key (`"sandbox_settings"`), which may cause state conflicts if both are declared in the same configuration. Remains unresolved.

4. **Resolved 2026-04-26.** `zia_cloud_app_control_rule` ISOLATE action requirements confirmed. Source: `vendor/terraform-provider-zia/docs/resources/zia_cloud_app_control_rule.md`. ISOLATE actions require a Cloud Browser Isolation (CBI) subscription and must be used alone — they cannot be mixed with other actions. A `cbi_profile` block is required when `action = "ISOLATE"`, referencing a `zia_cloud_browser_isolation_profile` data source. The `available_actions_without_isolate` computed attribute on the `zia_cloud_app_control_rule_actions` data source returns all valid non-ISOLATE actions for a given app category.

5. **Resolved 2026-04-26.** `zia_dc_exclusions` and `zia_sub_cloud` ID sourcing clarified. `zia_dc_exclusions` uses `datacenter_id` sourced from the `zia_datacenters` data source (e.g., `data.zia_datacenters.this.datacenter_id`). `zia_sub_cloud` uses `cloud_id` sourced from the `zia_sub_cloud` data source itself (read by name, then reference `data.zia_sub_cloud.lookup.id`). The two IDs refer to different entities — `datacenter_id` is a datacenter exclusion target; `cloud_id` is a subcloud (geographic traffic routing zone) ID. Sources: `vendor/terraform-provider-zia/docs/resources/zia_dc_exclusions.md` and `vendor/terraform-provider-zia/docs/resources/zia_sub_cloud.md`.

6. **Resolved 2026-04-26.** Admin rank values are fixed ZIA system values (not organization-specific). Source: `vendor/terraform-provider-zia/zia/resource_zia_admin_roles.go` line 62 — rank is a TypeInt with no validation constraint (accepts any int). The provider doc for `zia_admin_roles` states: "Default value is 7 (the lowest rank)." Per `vendor/zscaler-help/admin-rbac-captures.md`: rank controls which roles or admin users a given admin can manage — higher rank = more privilege. The valid range is 0–7 where 0 is the highest (super admin) rank and 7 is the default lowest rank.
