# Runtime data — never committed upstream by default. The public, non-secret
# mount layout lives in zscaler-skill-runtime.json; internal mirrors may switch
# it to a tracked work-mirror mount.
_data/

# Local setup defaults may contain private bootstrap source URLs.
zscaler-skill-setup.json

# Public directory contracts live in docs/data-contract/.

# Zscaler help material is committed (not ignored) — it's the pinned bibliography
# our references/*.md cite. Attribution and provenance live in
# vendor/zscaler-help/NOTICE. See that file and vendor/zscaler-help/README.md.

# Playwright MCP session artifacts — browser snapshots, console logs, screenshots
# captured during JS-rendered page fetches. Useful mid-session, not worth committing.
.playwright-mcp/

# Claude Code session state.
.claude/settings.local.json

# Secrets
.env
*.env
!.env.example

# Local/private reusable diagnostics.
_local-diagnostics/

# Local review scratch output.
full_diff.txt

# Python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.ruff_cache/

# macOS
.DS_Store

# Rendered PDF packets (scripts/render-skill-pdf.sh output)
dist/
