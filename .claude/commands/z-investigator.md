---
description: Start an evidence-based Zscaler troubleshooting investigation.
argument-hint: [what fails] in [where], [scope], since [when]
---

<!-- adapter-deps:start -->
Load and follow `agents/investigator/workflow.md`.
<!-- adapter-deps:end -->

If the `zscaler-investigator` MCP server is available, prefer its `investigate` prompt and tools per workflow.md's Transport selection section.

User input:
$ARGUMENTS
