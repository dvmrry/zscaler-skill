# Getting Started (Zscaler Client Connector API)

**Source:** https://help.zscaler.com/legacy-apis/getting-started-client-connector-api
**Captured:** 2026-04-26 via Playwright MCP (innerText extraction of article).

---

Your organization must meet the following prerequisites before you can access the Zscaler Client Connector API (`/zscaler-client-connector/understanding-zscaler-client-connector-api`):

- You must enable the API for your organization. To enable the API, contact Zscaler Support or submit a Zscaler Support ticket.
- Add an API key (`/zscaler-client-connector/adding-api-key`).

If you need to obtain API keys, authenticate into, and make calls using Zscaler OneAPI endpoints, see About API Clients in ZIdentity (`/zidentity/about-api-clients`) and Getting Started with OneAPI (`/oneapi/getting-started`).

After these prerequisites are met, you can:
1. Locate your base URI
2. Authenticate and create an API session
3. Make an API call

To learn more about rate limits and HTTP status codes, see Understanding Rate Limiting (`/zscaler-client-connector/understanding-rate-limiting`) and About Error Codes (`/zscaler-client-connector/about-error-codes`). If you encounter any issues with the API, contact Zscaler Support.

## Navigation (sidebar structure)

The ZCC (Zscaler Client Connector) section under `legacy-apis` contains:
- Understanding the Zscaler Client Connector API (`/legacy-apis/understanding-zscaler-client-connector-api`)
- API Developer & Reference Guide
  - Getting Started (`/legacy-apis/getting-started-client-connector-api`) ← this page
  - Understanding Rate Limiting (`/legacy-apis/understanding-rate-limiting-3`)
  - Understanding Error Codes (`/legacy-apis/about-error-codes`)
  - Reference Guide (expandable)
