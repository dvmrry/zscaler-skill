# Getting Started (Zscaler Cloud & Branch Connector API)

**Source:** https://help.zscaler.com/legacy-apis/getting-started-cloud-branch-connector-api
**Captured:** 2026-04-26 via Playwright MCP (snapshot extraction).

---

Your organization must meet the following prerequisites before you can access the Zscaler Cloud & Branch Connector API:

- You must enable the API for your organization. To enable the API, contact Zscaler Support or submit a Zscaler Support ticket.
- You must have your Admin Role (`/cloud-branch-connector/adding-cloud-connector-roles`) configured with **API Key Management** permission in the Zscaler Cloud & Branch Connector Admin Portal.
- You must add an API key (`/cloud-branch-connector/managing-organization-api-keys#AddNewKey`).

Once these prerequisites are met, you can:

1. Access your base URL and API key
2. Authenticate and create an API session
3. Make an API call
4. Activate configuration changes
5. Log out of API

To learn more about rate limiting and HTTP status codes, see Understanding Rate Limits (`/cloud-branch-connector/understanding-rate-limits`) and API Response Codes and Error Messages (`/cloud-branch-connector/api-response-codes-and-error-messages`). If you encounter any issues with the API, contact Zscaler Support.

## Navigation (sidebar structure)

The CBC (Cloud & Branch Connector) section under `legacy-apis` contains:
- Understanding the Zscaler Cloud & Branch Connector API (`/legacy-apis/understanding-zscaler-cloud-branch-connector-api`)
- API Developer & Reference Guide (expandable)
  - Getting Started (`/legacy-apis/getting-started-cloud-branch-connector-api`) ← this page
  - Configuring the Postman REST API Client (`/legacy-apis/configuring-postman-rest-api-client-3`)
  - API Response Codes and Error Messages (`/legacy-apis/api-response-codes-and-error-messages-2`)
  - Understanding Rate Limits (`/legacy-apis/understanding-rate-limits`)
  - Reference Guide (expandable)
  - Working with APIs (expandable)
