API ReferenceZscaler Digital Experience APITroubleshootingGet cloudpath metrics (latency, packet drops, etc
Get cloudpath metrics (latency, packet drops, etc
GET
https://api.zsapi.net/zdx/v1/devices/:deviceid/deeptraces/:trace_id/cloudpath-metrics

Get cloudpath metrics (latency, packet drops, etc.) from deeptrace session

Request
​
PATH PARAMETERS
deviceid
integer
REQUIRED

The device ID.

trace_id
string
REQUIRED

trace id

Responses
​
200
401
403
APPLICATION/JSON
Schema
Example (auto)
SCHEMA
Array [
leg_src
string

The leg source (client, egress, zen, connector, etc.).

leg_dst
string

The leg destination (egress, zen, destination, etc.).

stats
object[]
]
