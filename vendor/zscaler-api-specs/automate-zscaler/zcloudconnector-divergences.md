---
title: "DAV-21 automate.zscaler.com contract reconciliation — ZTW"
status: generated
generator: "scripts/automate-capture/reconcile_contract.py"
---

# automate.zscaler.com contract vs Go SDK / Python SDK / Terraform / Ansible / MCP — ZTW

> Generated by `scripts/automate-capture/reconcile_contract.py`. Do not edit by hand; re-run after re-capturing the contract or bumping the vendor submodules.

Diffs the rendered per-operation contract (`vendor/zscaler-api-specs/automate-zscaler/zcloudconnector-api-reference.json`) against the Go SDK struct, Python SDK model/request fields, Terraform provider schema, Ansible module argument specs, and Zscaler MCP server tools for each resource.

## Totals

- Type drift (contract vs Go primitive category): **0**
- Required drift (contract vs TF): **17**
- Enum: **12** match / **11** value-conflict / **4** one-sided
- Contract readonly fields checked: **0** (TF disagreement: 0)

- Ansible module surface: **0** present / **16** no surface
- Ansible required drift: **0**
- Ansible enum: **0** match / **0** value-conflict / **0** one-sided

- Python SDK surface: **12** present / **4** no surface
- Python SDK presence: **5** contract-unmatched / **4** python-only fields

- MCP tool surface: **6** present / **10** no surface (**3** with request-field surface)
- MCP request-field presence: **12** contract-unmatched / **0** MCP-only fields

## Contract Groups Outside Terraform Scope

Captured contract groups with no Terraform resource mapping in this report:

- `admin-and-role-management`
- `authentication`
- `cloud-branch-connector-groups`
- `public`
- `workload-groups`

## Scope Notes

- `resource_ztc_location_management.go` is not reconciled because `terraform-provider-ztc` does not register `ztc_location_management` in `ResourcesMap`; it is exposed only as a data source in the captured provider map.

## activation_status

`PUT /ztw/api/v1/ecAdminActivateStatus/activate` — contract 4 / Go 4 / TF 4 fields / no Ansible surface / Python 4 fields / no MCP surface

## account_group

`POST /ztw/api/v1/accountGroups` — contract 6 / Go 6 / TF 7 fields / no Ansible surface / Python 6 fields / no MCP surface

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

## dns_forwarding_gateway

`POST /ztw/api/v1/dnsGateways` — contract 10 / Go 11 / TF 8 fields / no Ansible surface / no Python surface / no MCP surface

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

**Enum value conflicts:**

- `ecDnsGatewayOptionsPrimary`: contract ['LAN_PRI_DNS_AS_PRI', 'LAN_SEC_DNS_AS_PRI', 'WAN_PRI_DNS_AS_PRI'] vs TF ['LAN_PRI_DNS_AS_PRI', 'LAN_SEC_DNS_AS_SEC', 'WAN_PRI_DNS_AS_PRI', 'WAN_SEC_DNS_AS_SEC']
- `ecDnsGatewayOptionsSecondary`: contract ['LAN_PRI_DNS_AS_SEC', 'LAN_SEC_DNS_AS_SEC', 'WAN_SEC_DNS_AS_SEC'] vs TF ['LAN_PRI_DNS_AS_PRI', 'LAN_SEC_DNS_AS_SEC', 'WAN_PRI_DNS_AS_PRI', 'WAN_SEC_DNS_AS_SEC']
- `failureBehavior`: contract ['FAIL_RET_ERR', 'FAIL_ALLOW_IGNORE_DNAT', 'FAIL_FORWARD_TO_ZTR'] vs TF ['FAIL_RET_ERR', 'FAIL_ALLOW_IGNORE_DNAT']

**Go SDK fields absent from the contract:** `type`

## dns_gateway

`POST /ztw/api/v1/dnsGateways` — contract 10 / Go 10 / TF 9 fields / no Ansible surface / no Python surface / no MCP surface

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

**Enum value conflicts:**

- `dnsGatewayType`: contract ['OFW_PDNS_REDIR_GW', 'EC_DNS_GW'] vs TF ['EC_DNS_GW']
- `ecDnsGatewayOptionsPrimary`: contract ['LAN_PRI_DNS_AS_PRI', 'LAN_SEC_DNS_AS_PRI', 'WAN_PRI_DNS_AS_PRI'] vs TF ['LAN_PRI_DNS_AS_PRI', 'LAN_SEC_DNS_AS_SEC', 'WAN_PRI_DNS_AS_PRI', 'WAN_SEC_DNS_AS_SEC']
- `ecDnsGatewayOptionsSecondary`: contract ['LAN_PRI_DNS_AS_SEC', 'LAN_SEC_DNS_AS_SEC', 'WAN_SEC_DNS_AS_SEC'] vs TF ['LAN_PRI_DNS_AS_PRI', 'LAN_SEC_DNS_AS_SEC', 'WAN_PRI_DNS_AS_PRI', 'WAN_SEC_DNS_AS_SEC']
- `failureBehavior`: contract ['FAIL_RET_ERR', 'FAIL_ALLOW_IGNORE_DNAT', 'FAIL_FORWARD_TO_ZTR'] vs TF ['FAIL_RET_ERR', 'FAIL_ALLOW_IGNORE_DNAT']

## forwarding_gateway

`POST /ztw/api/v1/gateways` — contract 13 / Go 19 / TF 12 fields / no Ansible surface / Python 12 fields / no MCP surface

**Required drift:**

- `primaryType`: contract required=False, TF required=True (TF stricter than API)
- `secondaryType`: contract required=False, TF required=True (TF stricter than API)

**Contract fields unmatched in the Python SDK model/request surface:** `type`

**Go SDK fields absent from the contract:** `dnsGatewayType`, `ecDnsGatewayOptionsPrimary`, `ecDnsGatewayOptionsSecondary`, `failureBehavior`, `primaryIp`, `secondaryIp`

## ip_destination_group

`POST /ztw/api/v1/ipDestinationGroups` — contract 11 / Go 8 / TF 8 fields / no Ansible surface / Python 10 fields / MCP 4 tools

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

**Contract fields unmatched in the Python SDK model/request surface:** `ipAddresses`

**Contract fields unmatched in MCP request tools:** `creatorContext`, `id`, `ipAddresses`, `ipCategories`, `isNonEditable`, `urlCategories`

**Contract fields absent from the Go SDK struct:** `creatorContext`, `ipAddresses`, `urlCategories`

## ip_pool_group

`POST /ztw/api/v1/ipGroups` — contract 6 / Go 8 / TF 5 fields / no Ansible surface / Python 6 fields / MCP 4 tools

**Required drift:**

- `ipAddresses`: contract required=False, TF required=True (TF stricter than API)
- `name`: contract required=False, TF required=True (TF stricter than API)

**Contract fields unmatched in MCP request tools:** `creatorContext`, `id`, `isNonEditable`

**Go SDK fields absent from the contract:** `extranetIpPool`, `isPredefined`

## ip_source_group

`POST /ztw/api/v1/ipSourceGroups` — contract 6 / Go 6 / TF 5 fields / no Ansible surface / Python 6 fields / MCP 4 tools

**Required drift:**

- `ipAddresses`: contract required=False, TF required=True (TF stricter than API)
- `name`: contract required=False, TF required=True (TF stricter than API)

**Contract fields unmatched in MCP request tools:** `creatorContext`, `id`, `isNonEditable`

## location_template

`POST /ztw/api/v1/locationTemplate` — contract 7 / Go 7 / TF 5 fields / no Ansible surface / Python 7 fields / no MCP surface

## network_service

`POST /ztw/api/v1/networkServices` — contract 11 / Go 11 / TF 11 fields / no Ansible surface / Python 12 fields / MCP 1 tools

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

**Python SDK fields absent from the contract:** `start`

## network_service_group

`POST /ztw/api/v1/networkServiceGroups` — contract 5 / Go 5 / TF 5 fields / no Ansible surface / Python 5 fields / MCP 1 tools

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

## provisioning_url

`POST /ztw/api/v1/provUrl` — contract 10 / Go 10 / TF 7 fields / no Ansible surface / Python 11 fields / no MCP surface

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

**Enum value conflicts:**

- `provUrlType`: contract ['ONPREM', 'CLOUD', 'DISABLED', 'MON_DELETED', 'DELETING', 'DELETING_LAST', 'DELETED'] vs TF ['ONPREM', 'CLOUD', 'DISABLED', 'MON_DELETED', 'DELETED']

**Python SDK fields absent from the contract:** `location`

## public_cloud_info

`POST /ztw/api/v1/publicCloudInfo` — contract 12 / Go 11 / TF 7 fields / no Ansible surface / Python 12 fields / MCP 1 tools

**Contract fields absent from the Go SDK struct:** `permissionStatus`

## traffic_forwarding_dns_rule

`POST /ztw/api/v1/ecRules/ecDns` — contract 38 / Go 21 / TF 17 fields / no Ansible surface / no Python surface / no MCP surface

**Required drift:**

- `name`: contract required=True, TF required=False (contract stricter than TF)

**Enum value conflicts:**

- `action`: contract ['ALLOW', 'BLOCK', 'REDIR_REQ', 'REDIR_RES', 'REDIR_ZPA', 'REDIR_REQ_DOH', 'REDIR_REQ_KEEP_SENDER', 'REDIR_REQ_TCP', 'REDIR_REQ_UDP', 'BLOCK_WITH_RESPONSE', 'BLOCK_WITH_EUN'] vs TF ['ALLOW', 'BLOCK', 'REDIR_ZPA', 'REDIR_REQ']

**Contract fields absent from the Go SDK struct:** `accessControl`, `blockResponseCode`, `departments`, `destCountries`, `destIpCategories`, `destIpv6Groups`, `deviceGroups`, `devices`, `groups`, `labels`, `redirectIp`, `resCategories`, `sourceCountries`, `sourceIpGroupExclusion`, `srcIpv6Groups`, `templateSyncedRuleFlag`, `timeWindows`, `users`

**Go SDK fields absent from the contract:** `type`

## traffic_forwarding_rule

`POST /ztw/api/v1/ecRules/ecRdr` — contract 46 / Go 42 / TF 28 fields / no Ansible surface / Python 45 fields / no MCP surface

**Required drift:**

- `forwardMethod`: contract required=False, TF required=True (TF stricter than API)
- `name`: contract required=True, TF required=False (contract stricter than TF)
- `order`: contract required=False, TF required=True (TF stricter than API)

**Enum value conflicts:**

- `forwardMethod`: contract ['INVALID', 'DIRECT', 'PROXYCHAIN', 'ZIA', 'ZPA', 'ECZPA', 'ECSELF', 'DROP', 'ENATDEDIP', 'GEOIP'] vs TF ['DIRECT', 'LOCAL_SWITCH', 'ZIA', 'ECZPA', 'DROP']

**Contract fields unmatched in the Python SDK model/request surface:** `accessControl`, `sourceCountries`, `templateSyncedRuleFlag`

**Python SDK fields absent from the contract:** `destWorkloadGroups`, `srcWorkloadGroups`

**Contract fields absent from the Go SDK struct:** `deviceGroups`, `devices`, `templateSyncedRuleFlag`, `timeWindows`, `zpaAppSegments`, `zpaGateway`

**Go SDK fields absent from the contract:** `defaultRule`, `srcWorkloadGroups`

## traffic_forwarding_log_rule

`POST /ztw/api/v1/ecRules/self` — contract 46 / Go 14 / TF 11 fields / no Ansible surface / no Python surface / no MCP surface

**Enum value conflicts:**

- `forwardMethod`: contract ['INVALID', 'DIRECT', 'PROXYCHAIN', 'ZIA', 'ZPA', 'ECZPA', 'ECSELF', 'DROP', 'ENATDEDIP', 'GEOIP'] vs TF ['ECSELF']

**Contract fields absent from the Go SDK struct:** `accessControl`, `appServiceGroups`, `blockResponseCode`, `departments`, `destAddresses`, `destCountries`, `destIpCategories`, `destIpGroups`, `destIpv6Groups`, `deviceGroups`, `devices`, `groups`, `labels`, `locationGroups`, `nwApplicationGroups`, `nwApplications`, `nwServiceGroups`, `nwServices`, `resCategories`, `sourceCountries`, `sourceIpGroupExclusion`, `srcIpGroups`, `srcIps`, `srcIpv6Groups`, `templateSyncedRuleFlag`, `timeWindows`, `users`, `wanSelection`, `zpaAppSegments`, `zpaApplicationSegmentGroups`, `zpaApplicationSegments`, `zpaBrokerRule`, `zpaGateway`

**Go SDK fields absent from the contract:** `defaultRule`

## Scope

Reconciles the contract against the Go SDK, Python SDK, Terraform provider, Ansible modules, and Zscaler MCP server tools where those surfaces exist. Python SDK comparison is presence-only because most mutable wrappers accept dynamic `**kwargs` and do not generally encode required or enum constraints. MCP comparison is also presence-only: it records resource/tool coverage and request fields the wrapper exposes, but does not infer required/readonly/enum constraints from natural-language tool descriptions. Resources without a mapped surface are marked explicitly. Field matching is conservative: exact names, with TF/Ansible/Python/MCP snake_case→camelCase derived from the source key; unmatched fields are reported as presence differences, never guessed.
