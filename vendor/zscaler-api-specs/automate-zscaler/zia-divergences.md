---
title: "DAV-21 automate.zscaler.com contract reconciliation — ZIA"
status: generated
generator: "scripts/automate-capture/reconcile_contract.py"
---

# automate.zscaler.com contract vs Go SDK / Terraform — ZIA

> Generated by `scripts/automate-capture/reconcile_contract.py`. Do not edit by hand; re-run after re-capturing the contract or bumping the vendor submodules.

Diffs the rendered per-operation contract (`vendor/zscaler-api-specs/automate-zscaler/zia-api-reference.json`) against the Go SDK struct and the Terraform provider schema for each resource.

## Totals

- Type drift (contract numeric vs Go string): **0**
- Required drift (contract vs TF): **10**
- Enum: **31** match / **3** value-conflict / **6** one-sided
- Contract readonly fields checked: **1** (TF disagreement: 0)

## bandwidth_class

`POST /zia/api/v1/bandwidthClasses` — contract 12 / Go 13 / TF 6 fields

**Go SDK fields absent from the contract:** `fileSize`

## ip_destination_group

`POST /zia/api/v1/ipDestinationGroups` — contract 9 / Go 8 / TF 8 fields

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

**Contract fields absent from the Go SDK struct:** `ipAddresses`, `urlCategories`

**Go SDK fields absent from the contract:** `isNonEditable`

## ip_source_group

`POST /zia/api/v1/ipSourceGroups` — contract 5 / Go 5 / TF 5 fields

**Required drift:**

- `ipAddresses`: contract required=False, TF required=True (TF stricter than API)
- `name`: contract required=False, TF required=True (TF stricter than API)

## location

`POST /zia/api/v1/locations` — contract 45 / Go 60 / TF 54 fields

**Enum value conflicts:**

- `profile`: contract ['NONE', 'CORPORATE', 'SERVER', 'GUESTWIFI', 'IOT'] vs TF ['NONE', 'CORPORATE', 'SERVER', 'GUESTWIFI', 'IOT', 'WORKLOAD', 'EXTRANET']

**Contract fields absent from the Go SDK struct:** `city`, `latitude`, `longitude`, `managedBy`

**Go SDK fields absent from the contract:** `basicAuthEnabled`, `childCount`, `digestAuthEnabled`, `dynamiclocationGroups`, `ecLocation`, `excludeFromDynamicGroups`, `excludeFromManualGroups`, `iotEnforcePolicySet`, `kerberosAuth`, `language`, `matchInChild`, `state`, `staticLocationGroups`, `subLocAccIds`, `subLocScope`, `subLocScopeEnabled`, `subLocScopeValues`, `virtualZenClusters`, `virtualZens`

## network_application_group

`POST /zia/api/v1/networkApplicationGroups` — contract 4 / Go 4 / TF 5 fields

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

## network_service

`POST /zia/api/v1/networkServices` — contract 10 / Go 11 / TF 11 fields

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

**Go SDK fields absent from the contract:** `protocol`

## network_service_group

`POST /zia/api/v1/networkServiceGroups` — contract 4 / Go 4 / TF 5 fields

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

## nss_server

`POST /zia/api/v1/nssServers` — contract 6 / Go 6 / TF 6 fields

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

**Enum value conflicts:**

- `status`: contract ['ENABLED', 'DISABLED', 'DISABLED_BY_SERVICE_PROVIDER', 'NOT_PROVISIONED_IN_SERVICE_PROVIDER', 'IN_TRIAL'] vs TF ['ENABLED', 'DISABLED']
- `type`: contract ['NONE', 'SOFTWARE_AA_FLAG', 'NSS_FOR_WEB', 'NSS_FOR_FIREWALL', 'VZEN', 'VZEN_SME', 'VZEN_SMLB', 'PINNED_NSS', 'MD5_CAPABLE', 'ADP', 'ZIRSVR', 'NSS_FOR_ZPA'] vs TF ['NSS_FOR_FIREWALL', 'NSS_FOR_WEB']

## risk_profile

`POST /zia/api/v1/riskProfiles` — contract 37 / Go 37 / TF 35 fields

**Required drift:**

- `profileName`: contract required=False, TF required=True (TF stricter than API)

## rule_label

`POST /zia/api/v1/ruleLabels` — contract 7 / Go 7 / TF 4 fields

## url_category

`POST /zia/api/v1/urlCategories` — contract 19 / Go 24 / TF 24 fields

**Required drift:**

- `id`: contract required=True, TF required=False (contract stricter than TF)

**Go SDK fields absent from the contract:** `categoryGroup`, `regexPatterns`, `regexPatternsRetainingParentCategory`, `urlType`, `val`

## user

`POST /zia/api/v1/users` — contract 10 / Go 12 / TF 10 fields

**Required drift:**

- `department`: contract required=False, TF required=True (TF stricter than API)

**Go SDK fields absent from the contract:** `authMethods`, `deleted`

## workload_group

`POST /zia/api/v1/workloadGroups` — contract 7 / Go 7 / TF 5 fields

## Scope

Reconciles the contract against the Go SDK and Terraform provider (the sources that carry type, required, readonly, and enum signal). Python SDK and Postman cross-checks are a documented next step. Field matching is conservative: exact names, with TF snake_case→camelCase derived from the TF key; unmatched fields are reported as presence differences, never guessed.
