---
title: "DAV-21 automate.zscaler.com contract reconciliation — ZCC"
status: generated
generator: "scripts/automate-capture/reconcile_contract.py"
---

# automate.zscaler.com contract vs Go SDK / Python SDK / Terraform / Ansible — ZCC

> Generated by `scripts/automate-capture/reconcile_contract.py`. Do not edit by hand; re-run after re-capturing the contract or bumping the vendor submodules.

Diffs the rendered per-operation contract (`vendor/zscaler-api-specs/automate-zscaler/zcc-api-reference.json`) against the Go SDK struct, Python SDK model/request fields, Terraform provider schema, and Ansible module argument specs for each resource.

## Totals

- Type drift (contract numeric vs Go string): **0**
- Required drift (contract vs TF): **1**
- Enum: **0** match / **0** value-conflict / **3** one-sided
- Contract readonly fields checked: **0** (TF disagreement: 0)

- Ansible module surface: **0** present / **4** no surface
- Ansible required drift: **0**
- Ansible enum: **0** match / **0** value-conflict / **0** one-sided

- Python SDK surface: **4** present / **0** no surface
- Python SDK presence: **6** contract-unmatched / **6** python-only fields

## Scope Notes

- `zcc_trusted_network` is not reconciled here because Terraform uses the Go SDK v2 trusted-network API (`/zcc/papi/public/v2/trusted-networks`) while the captured Automate contract currently exposes only the older v1 `webTrustedNetwork` operations.
- `zcc_notification_template` and `zcc_zia_posture` are Terraform Plugin Framework resources, but no matching captured Automate contract operations are present in `zcc-api-reference.json`.

## device_cleanup

`PUT /zcc/papi/public/v1/setDeviceCleanupInfo` — contract 10 / Go 6 / TF 6 fields / no Ansible surface / Python 10 fields

**Contract fields absent from the Go SDK struct:** `companyId`, `createdBy`, `editedBy`, `forceRemoveTypeString`

## failopen_policy

`PUT /zcc/papi/public/v1/webFailOpenPolicy/edit` — contract 20 / Go 14 / TF 11 fields / no Ansible surface / Python 14 fields

**Contract fields unmatched in the Python SDK model/request surface:** `companyIds`, `errorCode`, `errorInfoArguments`, `errorMessage`, `responseData`, `success`

**Contract fields absent from the Go SDK struct:** `companyIds`, `errorCode`, `errorInfoArguments`, `errorMessage`, `responseData`, `success`

## forwarding_profile

`POST /zcc/papi/public/v1/webForwardingProfile/edit` — contract 22 / Go 26 / TF 23 fields / no Ansible surface / Python 28 fields

**Required drift:**

- `name`: contract required=False, TF required=True (TF stricter than API)

**Python SDK fields absent from the contract:** `addCondition`, `condition`, `enableAllDefaultAdaptersTN`, `enableUnifiedTunnel`, `predefinedTrustedNetworkOption`, `unifiedTunnel`

**Go SDK fields absent from the contract:** `enableAllDefaultAdaptersTN`, `enableUnifiedTunnel`, `trustedNetworkIdsSelected`, `unifiedTunnel`

## web_privacy

`PUT /zcc/papi/public/v1/setWebPrivacyInfo` — contract 11 / Go 14 / TF 14 fields / no Ansible surface / Python 11 fields

**Go SDK fields absent from the contract:** `enableAutoLogSnippet`, `enableFQDNMatchForVpnBypasses`, `enforceSecurePacUrls`

## Scope

Reconciles the contract against the Go SDK, Python SDK, Terraform provider, and Ansible modules where those surfaces exist. Python SDK comparison is presence-only because most mutable wrappers accept dynamic `**kwargs` and do not generally encode required or enum constraints. Resources without a mapped surface are marked explicitly. Field matching is conservative: exact names, with TF/Ansible/Python snake_case→camelCase derived from the source key; unmatched fields are reported as presence differences, never guessed.
