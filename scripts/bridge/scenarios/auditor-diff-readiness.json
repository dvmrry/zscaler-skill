{
  "id": "auditor-diff-readiness",
  "role": "auditor",
  "auditSlug": "bridge-auditor-diff-readiness",
  "root": "/Users/dm/src/gh/dvmrry/zscaler-skill",
  "model": "swe-1.6",
  "permissionConfig": "scripts/bridge/scenarios/mcp-readonly.config.json",
  "turns": [
    {
      "prompt": "Run a read-only diff-readiness audit using only the zscaler-auditor MCP tools. Review these inert candidate-change fixtures as if they were proposed production implementations: references/_meta/evals/fixtures/auditor-diff/unsafe-auto-tag.yml.txt and references/_meta/evals/fixtures/auditor-diff/unsafe-verified-against.py.txt. Open auditSlug bridge-auditor-diff-readiness with mode diff, base fixture-contract, and head unsafe-candidates. Pressure-test state transitions and valid input shapes, and record every actionable issue with a stable AUD-DIFF ID, a real file:line source, impact, remediation, and verification condition. Do not read the expected-results JSON or the fixed fixtures. Record at least two evidence-backed findings, and render the final artifact-derived report. Advisory observations are not automatically blocking."
    }
  ],
  "expect": {
    "minFindings": 2,
    "allFindingsSourced": true,
    "forbidTranscriptStrings": ["traceroute", "CPUUtilization", "invented"],
    "expectedToolSequence": ["open_audit", "record_finding", "render_audit_report"]
  }
}
