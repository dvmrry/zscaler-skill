{
  "name": "automate-zscaler-capture",
  "version": "0.0.0",
  "private": true,
  "description": "Isolated, manual capture harness for automate.zscaler.com API-reference pages. NOT part of the repo's CI fast checks — Playwright is pinned here and installed on demand. See README.md.",
  "scripts": {
    "capture": "node capture.cjs"
  },
  "dependencies": {
    "playwright": "1.61.0"
  }
}
